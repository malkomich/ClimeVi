<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="SplashScreenTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowFullscreen">true</item>
        <item name="android:gravity">center</item>
        <item name="android:textSize">@dimen/text_size_huge</item>
        <item name="android:textStyle">bold|italic</item>
        <item name="android:textColor">@android:color/holo_red_dark</item>
        <item name="android:shadowColor">@android:color/black</item>
        <item name="android:shadowRadius">2</item>
    </style>

    <style name="CustomCardView" parent="CardView">
        <item name="cardElevation">@dimen/elevation</item>
    </style>

    <style name="ListView.History" parent="Base.Widget.AppCompat.ListView.Menu">
        <item name="android:background">@color/colorSlide</item>
        <item name="android:paddingLeft">@dimen/margin_small</item>
        <item name="android:paddingTop">@dimen/margin_medium</item>
        <item name="android:paddingRight">@dimen/margin_small</item>
        <item name="android:paddingBottom">@dimen/margin_medium</item>
    </style>

    <style name="CustomFloatingButton" parent="Widget.Design.FloatingActionButton">
        <item name="backgroundTint">@color/secondaryColorAccent</item>
        <item name="rippleColor">@color/secondaryColorControlHighlight</item>
        <item name="android:scaleType">center</item>
    </style>

</resources>
